{
  "name": "craftools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Professional editing & customization suite for stationery.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vite": "^8.1.4"
  }
}
