# Loaded by Vite automatically when running `npm run dev` or `vite build`.
# Only variables prefixed with VITE_ are exposed to client code / index.html
#
# API Base URL for craftools_api (Do NOT include trailing /v1)
VITE_API_BASE=http://localhost:8000
